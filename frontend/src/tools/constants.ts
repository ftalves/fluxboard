export const MOVE_THROTTLE_MS = 50;
export const TEXT_DEBOUNCE_MS = 300;
export const MIN_SIZE = 10;
export const DEFAULT_RECT_SIZE = { width: 100, height: 60 } as const;
export const DEFAULT_CIRCLE_SIZE = { width: 80, height: 80 } as const;
export const DEFAULT_TEXT_SIZE = { width: 100, height: 24 } as const;
